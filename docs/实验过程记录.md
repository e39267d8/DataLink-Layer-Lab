# 实验过程记录

> 对应《计算机网络实验一》**第 7 节 实验步骤**。建议每项写日期、参与人、现象与结论。

---

## 7.1 熟悉编程环境

- [ ] 复习滑动窗口理论（教材 + 讲义 ch3-Lab）
- [ ] 安装并打开 **Visual Studio**，能成功打开 `Lab1-Windows-VS2017\datalink.sln` 并 **生成** `datalink.exe`
- [ ] 运行教师 **Example** 中参考程序（如 `gobackn.exe`），双窗口 A/B，观察日志与利用率
- [ ] 记录：所用命令行、端口、是否 `-u` 无误码、日志文件名

**过程简述：**

（在此填写）

---

## 7.2 协议设计与程序总体设计

- [ ] 组内确定协议类型（不搭载 ACK 的 GBN / 搭载 ACK 的 GBN / 选择重传之一或多种）
- [ ] 定义帧字段（`include/datalink.h`）：`kind`、`seq`、`ack`、载荷与 CRC 边界约定
- [ ] 确定滑动窗口大小、序号空间、ACK 语义（累积确认或捎带等）
- [ ] 估算：重传定时器时限、ACK 定时器时限（指导书 6、11.3 要求定量分析，此处先记初值与迭代）

**设计要点记录：**

（在此填写）

---

## 7.3 编码与调试

- [ ] 在 `src/datalink.c` 实现 `wait_for_event` 主循环与各事件分支
- [ ] 实现 `send_frame` / `recv_frame` 与 `crc32` 的配合（指导书 8.9）
- [ ] 实现与网络层的 `enable` / `disable` 及与物理层 `PHYSICAL_LAYER_READY` 的配合（8.6、8.8）
- [ ] 定时器：`start_timer` / `stop_timer`、`start_ack_timer` / `stop_ack_timer`（8.10）

**主要 Bug 与解决：**

| 现象 | 原因 | 修复 |
|------|------|------|
|  |  |  |

---

## 7.4 软件测试与性能评价

按指导书 **第 9 节** 与 **表 3**（性能测试记录表）执行。下面为表 3 的简化清单，**请把每次运行的命令、时长、接收方利用率、问题抄到正式表格或 Excel**。

| 序号 | 场景概要 | A 端命令 | B 端命令 | 运行时间(s) | 利用率(%) | 问题 |
|------|----------|----------|----------|-------------|-----------|------|
| 1 | 无误码 | `datalink -u … A` | `datalink -u … B` |  |  |  |
| 2 | 默认平缓/周期 | `datalink … A` | `datalink … B` |  |  |  |
| 3 | 双端洪水 + 无误码 | `datalink -fu … A` | `datalink -fu … B` |  |  |  |
| 4 | 双端洪水 | `datalink -f … A` | `datalink -f … B` |  |  |  |
| 5 | 双端洪水 + 高误码 | `datalink -f -b 1e-4 …` | 同左 |  |  |  |

（指导书表 3 若多于 5 行，请按 PDF 补全。）

**结论要点：**

（在此填写：与理论利用率对比、参数调整记录）

---

## 7.5 实验报告与验收准备

- [ ] 按 `实验报告-提纲.md` 写完电子版报告
- [ ] 准备 **源程序清单**（仅自写 `.c/.h`，勿打包教师库若老师不要求）
- [ ] 整理可演示命令与日志片段，准备现场问答（指导书 10 节探索题可预习）

**验收前自检：**

（在此填写）
