# 赵博宇：实验任务清单（构建 · 过程记录 · 联调）

**学号：2024210908**

本组 **组长与协议主控** 仍为 **张恒基（2024210926）**；你主责「能编、能跑、能记、能交」，不修改 `datalink.c` / `datalink_recv.c` 核心逻辑。

---

## 一、代码与工程（主责）

| 项 | 说明 |
|----|------|
| `Makefile` | 保证 `make` 输出 `build/datalink`（或 `.exe`），`make clean` 可用 |
| `Lab1-Windows-VS2017/` | 验证 sln 生成 `build/datalink.exe`；**勿**在 Lab 目录改 `.c` |
| `docs/开发说明.md` | 编译路径、双站命令与 `build/` 目录约定 |
| Git | 分支命名、每日 PR 提醒（配合张恒基 17:00 合并窗口） |

---

## 二、实验过程记录（主责）

维护 **`docs/实验过程记录.md`** 第 7.1–7.5 节：日期、参与人、命令、现象。表 3 **场景 1、2、4** 由你执行并备份 `table3-*.log`；场景 3、5 由林旭东执行，你负责归档到附录目录。

---

## 三、报告段落

| 章节 | 内容 |
|------|------|
| 11.1 | 实验环境、工具链版本、`git` 短哈希 |
| 11.6 | 小组小结、提交物清单 |
| 附录 | 日志文件列表与路径说明 |

**不写**：11.2（2）（3）状态机、11.3 公式推导、11.4 探索题正文。

---

## 四、联调与验收

- 主持 **四人联调**（第 2 天）：双站 `-d3`、`--flood` / `--utopia`，记录是否 bad packet。
- 验收前：核对院系要求的纸质/电子版、演示命令一页纸（从 `build/` 运行）。

---

## 五、明确不做

- 不实现 `validate_and_process_frame` / `send_pure_ack`（尹浩铭）。
- 不统稿全文 `typst` 与表 3 汇总表（林旭东）。
- 不修改 GBN 发送窗口与 `phl_ready` 逻辑（张恒基）。

---

## 六、协作接口

- 向 **张恒基** 反馈：联调阻塞、构建失败、日志路径问题。
- 向 **林旭东** 提供：表 3 场景 1/2/4 的利用率数字与日志文件名。
- 向 **尹浩铭** 索取：接收侧异常日志样例（写入过程记录 7.3）。

详见 **`docs/四天四人方案.typ`**。
