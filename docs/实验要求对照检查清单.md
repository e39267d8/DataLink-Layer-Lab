# 实验要求对照检查清单

> 对照《计算机网络实验一》指导书，判断「课程要求」与「当前仓库状态」。  
> 状态说明：**已具备** / **进行中** / **待完成**（由组内勾选更新）。

---

## 一、指导书硬性目标

| 要求（节） | 说明 | 仓库/当前状态 |
|------------|------|----------------|
| 全双工、有噪音信道无差错传输（2、6） | A↔B 双向同时正确 | **已具备**：`src/datalink.c` 实现 GBN 事件循环与窗口发送；`src/datalink_recv.c` 实现接收校验与按序交付 |
| 利用带宽、参数可配（2、6、9） | 窗口、定时器、利用率 | **已具备**：`WINDOW_SIZE=3`、`DATA_TIMEOUT_MS=600`、`ACK_TIMEOUT_MS=200` 可在 `datalink.h` 中配置 |
| 可选协议难度（6） | 三种 GBN/SR 选型之一或多种 | **已具备**：搭载 ACK 的 Go-Back-N 协议 |

---

## 二、环境与工程（第 8 节）

| 项目 | 状态 |
|------|------|
| Windows：可编译 `datalink` | **已具备**：`Lab1-Windows-VS2017` 指向根目录 `src/`、`include/` |
| Linux：`make` | **已具备**：根目录 `Makefile` |
| 教师库未误删 | **已具备**：`protocol` / `lprintf` / `crc32` / `getopt` |
| 日志与 `-d` 调试 | **已具备**：库支持；需在自研代码中调用 `dbg_*` |

---

## 三、实验步骤与材料（第 7、9、11 节）

| 项目 | 状态 |
|------|------|
| 过程记录（7.1～7.5） | **已具备模板**：`docs/实验过程记录.md`，需填写真实过程 |
| 性能测试表 3（9 节） | **已具备**：五场景均已按 `-t 600` 跑满 10 分钟，数据已回填到 `docs/实验过程记录.md` 与 Typst 报告 |
| 实验报告（11 节） | **已具备**：`report.typ` 与 `docs/实验报告.typ` 已填入代码说明、参数分析、表 3、探索题与复盘 |
| 纸质 + 电子版提交 | **流程外**：按院系通知执行 |

---

## 四、与「实验够不够」的结论

**已够的部分（地基）**

- 目录与 **Windows 主工程**、**Linux Makefile**、**`.gitignore`**、**指导书对齐的 README**。
- **文档模板**：开发说明、实验过程、报告提纲、本检查清单。
- **参考程序**：各 `Lab*/Example/` 下教师样例可对照行为（路径以你本机仓库为准）。

**仍必须由组内完成的部分（核心得分点）**

1. ~~**`src/datalink.c` 完整协议逻辑**（否则实验主体不成立）。~~ ✅ **已完成**（GBN 事件循环 + 窗口发送 + 重传 + 流量控制）
2. ~~**`include/datalink.h` 与协议一致的帧与常量**。~~ ✅ **已完成**（帧结构、`WINDOW_SIZE=3`、`MAX_SEQ=255`、定时器参数）
3. ~~**表 3 多场景测试数据** + **11.3 理论推导与对比**。~~ ✅ **已完成**（五场景 600 秒真实数据，均无坏分组）
4. ~~**实验报告全文** + **第 10 节选做讨论**。~~ ✅ **已完成**（11.4 CRC 与定时器两题，11.5 补充真实复盘）
5. **验收演示**与可能的 **纸质材料**。（流程外：按院系通知打印/提交）

若以上「待完成」均勾选为已完成，且长时间运行无网络层报错，则与指导书实验要求对齐度较高。
